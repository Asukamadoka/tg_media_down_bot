# MTProto client. Pinned below 2.0 because Telethon 2.x is a breaking rewrite.
Telethon>=1.41,<2.0
# Optional but strongly recommended: C implementation of the MTProto crypto,
# which roughly triples download throughput.
cryptg>=0.5
# PikPak cloud API (async, httpx based).
pikpakapi==0.1.11
# Serves downloaded files over HTTP so PikPak can fetch them itself.
aiohttp>=3.9
PyYAML>=6.0
python-dotenv>=1.0
# --- pikpak_wms: the PikPak warehouse package (see docs/wms/) ---
# Rule files, plans and the natural-language Query are validated with pydantic.
pydantic>=2.7,<3
# The `wms` command line; pulls in rich for its tables.
typer>=0.12,<1
# Scheduled stocktake / organize runs. 4.x is a rewrite with another API.
APScheduler>=3.10,<4
# Time-zone data for zoneinfo, in case the slim base image lacks it.
tzdata>=2024.1
# Natural-language commands (docs/wms/M6): the Claude translator backend.
# Only called when NL_BACKEND or NL_FALLBACK is "claude".
anthropic>=1.8,<2
